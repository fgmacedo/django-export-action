============
Installation
============

At the command line::

    $ easy_install django-export-action

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-export-action
    $ pip install django-export-action
